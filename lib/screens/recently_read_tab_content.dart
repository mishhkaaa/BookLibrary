import 'package:flutter/material.dart';

class RecentlyReadTabContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Recently Read Tab Content'),
    );
  }
}
